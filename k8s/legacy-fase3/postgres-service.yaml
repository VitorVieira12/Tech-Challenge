apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: tech-challenge
  labels:
    app: postgres
spec:
  type: ClusterIP  # Apenas acessível dentro do cluster
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: postgres



