apiVersion: v1
kind: Secret
metadata:
  name: newrelic-secret
  namespace: tech-challenge
  labels:
    app: tech-challenge
type: Opaque
stringData:
  # Valor real injetado pelo CI/CD via: kubectl create secret generic newrelic-secret ...
  # Não commitar o valor real aqui - use o GitHub Secret NEW_RELIC_LICENSE_KEY
  NEW_RELIC_LICENSE_KEY: "PLACEHOLDER_REPLACED_BY_CICD"
